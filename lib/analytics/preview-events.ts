export function trackEvent(eventName: string, properties?: Record<string, unknown>) {
  // Placeholder tracking hook for preview funnel optimization.
  console.log('[PreviewTracking]', eventName, properties ?? {})
}
