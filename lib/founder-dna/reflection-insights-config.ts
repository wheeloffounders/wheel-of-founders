/**
 * @deprecated Import from `@/lib/founder-dna/unlock-schedule-config` for new code.
 * Celebration Gap / Recurring Question gates use **account age (days)**, not evening count.
 */
export {
  CELEBRATION_GAP_MIN_DAYS,
  RECURRING_QUESTION_MIN_DAYS,
  POSTPONEMENT_MIN_DAYS,
} from '@/lib/founder-dna/unlock-schedule-config'
