/** Shared Mrs. Deer insight “letter” frame — morning, plan review, profile. */
export const morningInsightSurfaceClassName =
  'rounded-2xl border-2 border-dashed border-amber-200 bg-amber-50/30 p-6 shadow-sm dark:border-amber-800/55 dark:bg-amber-950/10'

/** @deprecated Use `morningInsightSurfaceClassName` — kept for stale HMR bundles. */
export const MORNING_INSIGHT_SURFACE = morningInsightSurfaceClassName

/** In-card streaming / generating banner (post-morning audit, profile save). */
export const morningInsightStreamingBannerClassName =
  'rounded-xl border border-dashed border-amber-200/80 bg-amber-50/40 px-4 py-3 dark:border-amber-800/40 dark:bg-amber-950/25'
