'use client'

import Link from 'next/link'
import { Sunrise } from 'lucide-react'

export function MorningLoopOpenBanner() {
  return null
  /*
  return (
    <div className="sticky top-0 z-[40] border-b border-amber-200/80 bg-gradient-to-r from-amber-50 via-white to-amber-50/90 px-3 py-2 shadow-sm dark:border-amber-800/50 dark:from-amber-950/40 dark:via-gray-900 dark:to-amber-950/30">
      <div className="mx-auto flex max-w-4xl flex-wrap items-center justify-center gap-2 text-center sm:justify-between sm:text-left">
        <p className="flex items-center gap-2 text-xs font-medium text-amber-950 dark:text-amber-100 sm:text-sm">
          <Sunrise className="h-4 w-4 shrink-0 text-amber-600 dark:text-amber-400" aria-hidden />
          <span>Your Morning Loop is still open.</span>
        </p>
        <Link
          href="/morning"
          className="shrink-0 rounded-lg bg-[#ef725c] px-3 py-1.5 text-xs font-semibold text-white shadow-sm transition hover:opacity-95 sm:text-sm"
        >
          Resume planning
        </Link>
      </div>
    </div>
  )
  */
}
