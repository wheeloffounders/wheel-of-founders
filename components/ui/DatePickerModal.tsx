'use client'

/**
 * Month picker modal for Morning / Evening / Emergency page headers.
 * Re-uses the shared calendar grid + status dots.
 */
export { CalendarModal as DatePickerModal } from '@/components/CalendarModal'
